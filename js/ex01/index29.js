/* Create a new index29.js file
Calculate and show the perimeter of a square
Side length is 10
Show a descriptive message to show the square perimeter and side length*/
let sideLenght = 10;
const squarePrimeter = 4;
let result = sideLenght * squarePrimeter
let templateforperimeterofasquare = `If the side lenght of a square is ${sideLenght}, then you multiply it by ${squarePrimeter}
and the result would be ${result}.`;
console.log(templateforperimeterofasquare);
